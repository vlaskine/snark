#
# This module is provided as rur USE_FILE by rur_config.cmake.  It can
# be INCLUDEd in a project to load the needed compiler and linker
# settings to use Utility Functions.
#

IF(NOT rur_USE_FILE_INCLUDED)
  SET(rur_USE_FILE_INCLUDED 1)

  # Add compiler flags needed to use rur.
  #SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${rur_REQUIRED_C_FLAGS}")
  #SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${rur_REQUIRED_CXX_FLAGS}")
  #SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${rur_REQUIRED_EXE_LINKER_FLAGS}")
  #SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${rur_REQUIRED_SHARED_LINKER_FLAGS}")
  #SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} ${rur_REQUIRED_MODULE_LINKER_FLAGS}")

  # Add include directories needed to use rur
  INCLUDE_DIRECTORIES(${rur_INCLUDE_DIRS})

  # Add link directories needed to use rur.
  LINK_DIRECTORIES(${rur_LIBRARY_DIRS})

IF( WIN32 )
  ADD_DEFINITION( NOMINMAX )
ENDIF(WIN32)

ENDIF(NOT rur_USE_FILE_INCLUDED)
